# MySQL Configuration
MYSQL_USER=Siddharth
MYSQL_PASSWORD=siddharth_21A
MYSQL_HOST=db
MYSQL_PORT=3306
MYSQL_DB=maikms
MYSQL_ROOT_PASSWORD=rootpassword

# Qdrant Configuration (server mode, not local file)
QDRANT_HOST=qdrant
QDRANT_PORT=6333
USE_LOCAL_QDRANT=false

# Ollama (running on host machine, accessed via Docker host)
OLLAMA_API_URL=http://host.docker.internal:11434/api/generate

# Security
SECRET_KEY=your-production-secret-key-change-this

# CORS
BACKEND_CORS_ORIGINS=["http://localhost","http://localhost:3000","http://localhost:80"]
